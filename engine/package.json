{
  "name": "electrician-sim-engine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "电工模拟器 · 电路连通性判定引擎（第一版状态模拟）",
  "scripts": {
    "test": "node --test test/*.test.ts",
    "test:watch": "node --test --watch test/*.test.ts"
  },
  "engines": {
    "node": ">=22.6.0"
  }
}
